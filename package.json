{
  "name": "remote-job-assistant",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "build": "npm run build --prefix client",
    "test": "npm test --prefix server && npm run lint --prefix client"
  },
  "devDependencies": {
    "concurrently": "latest"
  }
}

